<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Home Page</title>
  <style>
    /* Reset & Base */
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f5f7fa;
      margin: 0;
      padding: 0;
      color: #2c3e50;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    /* Header */
    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: #34495e;
      padding: 18px 40px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.15);
    }

    h2 {
      font-size: 28px;
      font-weight: 700;
      color: #ecf0f1;
      margin: 0;
      user-select: none;
    }

    nav {
      display: flex;
      gap: 14px;
      align-items: center;
    }

    .btn, .logout {
      font-weight: 600;
      font-size: 15px;
      border-radius: 6px;
      padding: 11px 18px;
      border: none;
      cursor: pointer;
      box-shadow: 0 3px 6px rgba(0,0,0,0.12);
      transition: all 0.3s ease;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      user-select: none;
    }

    .btn {
      background-color: #27ae60;
      color: white;
      border: 1.5px solid #27ae60;
    }
    .btn:hover {
      background-color: #219150;
      border-color: #219150;
      box-shadow: 0 6px 12px rgba(33, 145, 80, 0.4);
    }

    .logout {
      background-color: #e74c3c;
      border: 1.5px solid #e74c3c;
      color: white;
    }
    .logout:hover {
      background-color: #c0392b;
      border-color: #c0392b;
      box-shadow: 0 6px 12px rgba(192, 57, 43, 0.4);
    }

    /* Main content */
    main {
      max-width: 900px;
      margin: 40px auto;
      padding: 0 20px 40px;
    }

    h1 {
      font-weight: 700;
      font-size: 36px;
      margin-bottom: 35px;
      color: #34495e;
      user-select: none;
    }

    ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .event-card {
      background: white;
      margin-bottom: 24px;
      padding: 25px 30px;
      border-radius: 12px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.08);
      display: flex;
      flex-direction: column;
      gap: 8px;
      user-select: none;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .event-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 30px rgba(0,0,0,0.12);
    }

    .event-card strong {
      color: #2c3e50;
      font-size: 18px;
    }

    .book-btn {
      background-color: #f1c40f;
      color: #2c3e50;
      padding: 10px 16px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      margin-top: 12px;
      width: fit-content;
      transition: background-color 0.3s ease, box-shadow 0.3s ease;
      box-shadow: 0 4px 8px rgba(241, 196, 15, 0.3);
    }

    .book-btn:hover {
      background-color: #d4ac0d;
      box-shadow: 0 6px 14px rgba(212, 172, 13, 0.5);
    }

    @media (max-width: 600px) {
      header {
        flex-direction: column;
        gap: 15px;
        padding: 20px;
      }
      main {
        margin: 30px 15px 40px;
      }
      .event-card {
        padding: 20px;
      }
    }
  </style>
</head>
<body>
  <header>
    <h2>EventHub</h2>
    <nav>
      <a href="/mytickets" class="btn">🎫 View My Tickets</a>
      <form method="get" action="/logout" style="margin: 0;">
        <button type="submit" class="logout">🚪 Log Out</button>
      </form>
    </nav>
  </header>

  <main>
    <h1>View All Events</h1>
    <ul>
      <% for(let e of event){ %>
        <li class="event-card">
          <p><strong>📌 Title:</strong> <%= e.title %></p>
          <p><strong>📝 Description:</strong> <%= e.description %></p>
          <p><strong>📅 Date:</strong> <%= e.date %> &nbsp; <strong>⏰ Time:</strong> <%= e.time %></p>
          <p><strong>📍 Location:</strong> <%= e.location %></p>
          <p><strong>🪑 Total Seats:</strong> <%= e.total_seats %> &nbsp; <strong>🎟️ Available:</strong> <%= e.available_seats %></p>
          <a href="/book/<%= e.id %>" class="book-btn">✅ Book Ticket</a>
        </li>
      <% } %>
    </ul>
  </main>

</body>
</html>
